<Window x:Class="University.View.Common.DesktopWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:University.View.Common"
        xmlns:vm="clr-namespace:University.ViewModel.Common"
        mc:Ignorable="d"
        Title="Рубцовский индустриальный институт" Height="450" Width="800" WindowState="Maximized" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:DesktopViewModel/>
    </Window.DataContext>
    <Grid>
        
        <StatusBar
            Height="21"
            FlowDirection="RightToLeft"
            VerticalAlignment="Bottom">
            <StackPanel
                Orientation="Horizontal">
                <TextBlock
                    Text="{Binding Path=UserFullName}"
                    Margin="15,0"/>
            </StackPanel>
        </StatusBar>
    </Grid>
</Window>
